<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.riske.remindeer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Welcome.BienvenidaActivity"
            android:theme="@style/AppThemeStatusBarBlue"></activity>
        <activity android:name=".Welcome.ForgotPasswordActivity" />
        <activity android:name=".Welcome.LoginActivity" />
        <activity android:name=".Splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Splash.TestViewPagerActivity"></activity>
        <activity
            android:name=".Welcome.RegisterActivity"
            android:theme="@style/AppThemeStatusBarBlue" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeStatusBarBlue">
        <intent-filter>
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <receiver
            android:name=".Notifications.AlarmReceiver"
            android:enabled="true" />

        <service
            android:name=".Notifications.NotificationService"
            android:enabled="true" />

        <receiver
            android:name=".Notifications.AlarmReceiver1"
            android:enabled="true" />

        <service
            android:name=".Notifications.NotificationService1"
            android:enabled="true" />

        <receiver
            android:name=".Notifications.AlarmReceiver2"
            android:enabled="true" />

        <service
            android:name=".Notifications.NotificationService2"
            android:enabled="true" />

        <receiver
            android:name=".Notifications.AlarmReceiver3"
            android:enabled="true" />

        <service
            android:name=".Notifications.NotificationService3"
            android:enabled="true" />


    </application>

</manifest>